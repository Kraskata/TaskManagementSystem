@model IEnumerable<AssignmentIndexServiceModel>

@{
    ViewData["Title"] = "Home Page";
    var assignments = Model.ToList();
}

<style>
    .card {
        border: 1px solid #ccc;
        border-radius: 5px;
        padding: 10px;
        margin: 10px;
        width: 300px;
        display: inline-block;
    }

    .card h3 {
        margin-top: 0;
    }

    .card p {
        margin-bottom: 5px;
    }

    .card .completed {
        color: green;
    }

    .card .not-completed {
        color: red;
    }
</style>

@if (!assignments.Any())
{
    <div class="mt-4 p-5 bg-light">
        <h1 class="display-4">Here are the newest tasks available.</h1>
        <p class="lead">
            There are no assignments assigned. 
            In case you need to assign one feel free to do so.
        </p>
        <hr class="my-4">
        <p class="lead">
            @if (User?.Identity?.IsAuthenticated ?? true)
            {
                <a asp-controller="Assignment" asp-action="Add" class="btn btn-primary btn-lg" role="button">Add an Assignment</a>
            }
        </p>
    </div>
}

    <div>
        @for (int i = 0; i < assignments.Count(); i++)
        {
            var assignment = assignments[i];
            <div class="card">
                        <a class="btn btn-primary" asp-controller="Assignment" asp-action="Details"
                       asp-route-id="@assignment.Id"> @assignment.Title</a>
                <p>@assignment.Description</p>
                <p>Category: @assignment.Category.Name</p>
            </div>
        }
    </div>